RewriteEngine On
RewriteBase /

# If file/folder exists, don't redirect
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirect all to index.php
RewriteRule ^(.*)$ index.php [QSA,L]
